# Setup

## Prerequisites

- Node.js 18 or newer
- npm
- MongoDB is optional for the current mock API phase

## Frontend

```bash
cd frontend
npm.cmd install
npm.cmd run dev
```

## Backend

```bash
cd backend
npm.cmd install
npm.cmd run dev
```

Create `.env` files from `.env.example` when connecting real services.
