# Contributing

- Use feature branches for new work.
- Keep frontend and backend changes separated when possible.
- Run `npm.cmd run build` in `frontend` before submitting UI changes.
- Document new API endpoints in `docs/API_SPEC.md`.
